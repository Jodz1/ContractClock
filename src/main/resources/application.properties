# Recka database settings
# Copy this file to ~/.recka/application.properties if you want local overrides.
db.url=jdbc:mysql://localhost:3306/time_tracker_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=nova_lozinka

# Safety default: false means Recka will not create, seed or modify the database on startup.
db.autoInitialize=false

app.defaultExportFolder=exports
app.defaultCurrency=USD
